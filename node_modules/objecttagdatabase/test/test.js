'use strict';

var expect = require('chai').expect;
var objectTagDatabase = require('../index.js');

describe('Basic Printing String Test', function() {
    it('Should print a messeage', function() {
        var result = objectTagDatabase.printMsg();
        expect(result).to.equal(0);
    });
});